<% if @feed_items.any? %>
  <ol class="microposts">
  	<!-- Pokud zadavame parametry, pak odkaz na partial musime explicitne zminit pres partial: -->
    <%= render partial: 'shared/feed_item', collection: @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>